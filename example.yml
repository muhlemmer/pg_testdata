dsn: dbname=testdb user=testuser password=xxx host=localhost port=5432 sslmode=require
  fallback_application_name=pg_testdata connect_timeout=10
tables:
- name: articles
  amount: 10000
  columns:
  - name: published
    seed: 2
    nullprobability: 0
    type: bool
    generator:
      probability: 70
